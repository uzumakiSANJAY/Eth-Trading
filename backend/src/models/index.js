const Ohlcv = require('./ohlcv.model');
const Indicator = require('./indicator.model');
const Pattern = require('./pattern.model');
const Signal = require('./signal.model');
const PaperTrade = require('./paperTrade.model');
const PaperAccount = require('./paperAccount.model');

module.exports = {
  Ohlcv,
  Indicator,
  Pattern,
  Signal,
  PaperTrade,
  PaperAccount,
};
